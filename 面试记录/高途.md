##### 一面
1、水平居中方案
2、script标签加载，async和defer
3、首屏优化，首屏指标,如何优化
4、宏任务和微任务，做题输出
5、模块化
6、webpack
7、微前端，模块打包
8、fiber
9、react hooks
10、防抖节流
11、promise以及相关API，.then为什么可以链式调用
12、如何控制多环境打包
13、算法题
输出所有和为N的连续正数序列.
输入一个正数N, 输出所有和为N的连续正数序列. 例如输入15, 结果: [[1, 2, 3, 4, 5], [4, 5, 6], [7, 8]]
```
 function fn(value) {
     if(value === 0) return []
     if(value === 1) return [[1]]
     const res = []
     const halfValue = Math.floor(value/2)+1
     for(let i = 1;i<halfValue;i++){
         let current = i+1
         let total = i+current
         let curArr = [i]
         let flag = false
          while(total<value+1&&!flag){
                if(total === value){
                    curArr.push(current)
                    res.push(curArr)
                    flag = true
                }else{
                    curArr.push(current)
                    current++
                    total = total+current
                }
             }
     }
     return res
 }
 fn(15)
```